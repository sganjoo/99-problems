pipeline {
    stages {
        stage('build') {
            steps {
                sh “echo build step executing”
            }
        }
	stage(‘test’) {
            steps {
                print ‘test step executing’
            }
        }
    }
}
